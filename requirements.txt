# TradingAgents OpenClaw Skill — 依赖
# 安装：pip install -r requirements.txt

# ─── 核心（signals engine 用）─────────────────────────
numpy>=1.24.0
pandas>=2.0.0
yfinance>=0.2.40
requests>=2.31.0

# 测试
pytest>=7.4.0

# ─── 可选：LLM engine 启用 TradingAgents ──────────────
# 启用 LLM 决策时安装：
#   pip install tradingagents
# 或者本地 clone TradingAgents-Official 后设：
#   export TRADING_AGENTS_HOME=/path/to/TradingAgents-Official
#
# TradingAgents 自身的依赖（仅当用 LLM engine 时需要）：
# langgraph>=0.2.0
# langchain-core>=0.3.0
# langchain-openai>=0.2.0
# openai>=1.0.0
# anthropic>=0.40.0
# google-generativeai>=0.8.0
# stockstats>=0.5.0
# backtrader

# ─── 可选：环境加载 ──────────────────────────────────
python-dotenv>=1.0.0
